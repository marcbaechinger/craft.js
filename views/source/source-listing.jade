if context === config.context.src
	h2.collapse Annotated source code of #{path} #{context}
		.size #{sourceCode.length} characters / #{lines.length} lines
else if context === config.context.dist
	h2.collapse Released file: #{path}
		.size #{sourceCode.length} characters / #{lines.length} lines
else if context === config.context.jobs
	h2.collapse Build definition of #{path}

.source-panel
	if displayMode === "js"
		.marker-button(data-action="toggle-source-markers") FIXME/TODO
		ul#markers
	.source
		each line, i in lines 
			if typeof report === "undefined" || typeof report.errors==='undefined' || typeof report.errorLines[i+1] === 'undefined'
				pre(id="line-#{i+1}") #{i+1}:\t#{line}
			else
				pre.error(id="line-#{i+1}") #{i+1}:\t#{line}
					.reason #{report.errorLines[i+1]}
